#include "main.h"

/**
 * print_chessboard - prints chess bpard
 * @a: some text
 * Return: returns nothing
 */

void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; a[i][j]; i++)
	{
		for (j = 0; a[i][j]; j++)
		{
			_putchar(a[i][j]);
		}
		_putchar('\n');
	}
}
