#include "main.h"

/**
 * _strcmp - writes the character c to stdout
 * @s1: parameter 1
 * @s2: parameter 2
 *
 * Return: type char pointer
 */

int _strcmp(char *s1, char *s2)
{
	int diff;

	diff = s1[0] - s2[1];

	return (diff);
}
