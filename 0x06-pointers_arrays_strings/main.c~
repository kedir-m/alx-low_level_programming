#include <stdio.h>
#include "main.h"

/**
 * main - checks code
 *
 * Return: returns 0 Always
 */

int main(void)
{
	char s1[98] = "Hello ";
	char s2[] = "World!\n";
	char *ptr;

	printf("%s\n", s1);
	printf("%s", s2);
	return (0);
}
