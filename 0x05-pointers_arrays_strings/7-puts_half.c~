#include "main.h"

/**
 * rev_string - sometext
 * @s: sometext
 * Return: some too
 */

void puts_half(char *str)
{
	int i, j, n;

	i = 0;
	while(str[i])
	{
		i++;
	}
	n = i / 2;
	j = i / 2;
	while (n--)
	{
		_putchar(str[j]);
		j++;
	}
}
