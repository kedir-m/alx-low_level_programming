#include <stdio.h>
#include "main.h"

/**
 * _strlen - sometext
 * @s: sometext
 * Return: some too
 */

int _strlen(char *s)
{
	int n, i, sum;

	n = sizeof(s);
	sum = 0;

	for (i = 0; i < n; i++)
	{
		sum += s[i];
	}

	return (0);
}
